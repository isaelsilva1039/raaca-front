// next.config.js

/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['127.0.0.1'], // Adicione todos os domínios dos quais você carrega imagens
    },
  }
  
  module.exports = nextConfig;
  